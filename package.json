{
  "name": "browserzs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "postinstall": "npx playwright install chromium --with-deps 2>/dev/null || true"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}
